# DIU Question Bank Web

Default Next.js App Router starter configured for Cloudflare Workers through OpenNext.

From the repository root:

```bash
pnpm dev:web
pnpm --filter @diuqbank/web preview
pnpm deploy:web
```

The site intentionally contains only the default `create-next-app` page for now.
