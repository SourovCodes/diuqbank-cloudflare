VITE_API_BASE_URL=/api
VITE_DEV_API_PROXY_TARGET=https://diuqbank-api-prod.sourov-cse.workers.dev

# Google tag IDs, injected into index.html for `vite build` only (never the dev
# server). Public values, not secrets. Defaults live in vite.config.ts; set
# these to override per environment.
VITE_GA_MEASUREMENT_ID=G-QPKSEMRTZ2
VITE_ADSENSE_CLIENT_ID=ca-pub-4157128010679783
