# Base URL of the DIU Question Bank API the web app calls.
# Copy to `.env.local` for local development (e.g. point at a local `pnpm dev:api`).
# When unset, the app falls back to the deployed Worker in src/lib/api.ts.
VITE_API_URL=http://localhost:8787
